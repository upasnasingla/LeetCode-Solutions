import java.util.*;
public class Main
{
	public static void main(String[] args) {
		int arr[]={1,8,5,6,2,1,7,8,10};
		Arrays.sort(arr);
		HashSet<Integer>h=new HashSet<>();
		for(int i=0;i<arr.length;i++){
		    h.add(arr[i]);
		}
		ArrayList<Integer>a=new ArrayList<>(h);
		for(int i=0;i<a.size();i++){
		    System.out.println(a.get(i));
		}
		
	}
}
